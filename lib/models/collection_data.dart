class CollectionData {
  double progressionPercentage;
  int totalCardsOwned;
  int doubledCards;
  int missingCards;
  int boostersBought;

  CollectionData({
    required this.progressionPercentage,
    required this.totalCardsOwned,
    required this.doubledCards,
    required this.missingCards,
    required this.boostersBought
  });
}
